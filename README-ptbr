Esta é a Versão 1.33 estável da biblioteca "Free JPDF Pascal".

A biblioteca "Free JPDF Pascal" foi escrita totalmente em "Free Pascal" puro. 
Ela foi criada a partir da conversão e readaptação do código da 
biblioteca FPDF escrita em PHP (http://www.fpdf.org).

Obs: Esta versão trabalha com codificação ISO-8859-1. 
     Assim, se o texto ou o arquivo contendo o texto estiver em UTF8, 
     insira no código SetUTF8(True) antes de usar as funções de texto.
     Se ainda assim não funcionar, usando um editor de texto, por exemplo, 
     Geany (Linux/Windows) ou Notepad++ (Windows), converta o arquivo
     que contém o código fonte que constroi o seu relatório para a 
     codificação ISO-8859-1.
     Lembre-se, isto só é necessário se o texto contiver acentos.

Recursos:
- Suporte a Imagens (JPG ou JPEG, PNG, GIF e BMP);
- Suporte as principais fontes (Helvetica [Arial], Times, Courier, Symbol e Zapfdingbats);
- Suporte a estilos (negrito, itálico e/ou sublinhado);
- Vários formatos de papel (A3, A4, A5, Legal e Letter);
- Escolher unidade de medida (centímetro, milímetro, pontos e polegadas);
- Quebra automática de página ou não;
- Texto com todos os tipos de alinhamentos;
- Poder carregar um arquivo de texto que será incluído no PDF, 
  com quebra de linhas e páginas automáticos (justificado, 
  esquerda, direita ou centro);
- Escrever texto livre (em qualquer posição na folha);
- Criar tabelas com o método Cell;
- Suporte a cor de linha, fundo e texto;
- Suporte a linhas de bordas;
- Possibilidade de trocar a orientação (retrato ou paisagem) 
  das páginas no mesmo documento;
- Suporte a compactação (gera PDF menores);
- Métodos para desenhar retângulos e linhas;
- Suporte a Cabeçalhos e Rodapés (para usar é preciso criar uma 
  classe filha, assim como na FPDF em PHP);
- Gera códigos de barras para boletos bancários;

Novidades na Versão 1.2:
- Corrigido método Code25 que estava gerando códigos de barras errados;

Novidades na Versão 1.1:
- Corrigido problema com sublinhado de tamanho irregular;
- Corrigido problema com sublinhado em MultiCell;
- Corrigido problema com texto menor que uma linha em MultiCell;

Novidades na Versão 1.0 Estável:
- O método fpdf mudou para Create;
- Não é mais necessário chamar o método Open depois do Create;
- Uso enumeradores em vários métodos (Gilson Nunes);
- Resolvido problema com o ponto decimal no Windows (Gilson Nunes);
- Reduzido o número de IF's (vindos da FPDF) devido ao uso de enumeradores;
- Alguns métodos internos foram removidos, pois se tornaram desnecessários;
- Uso de nomes de variáveis mais legíveis;
- Suporte a cores padrão (cBlack, cSilver, cRed, etc);
- Suporte a texto sublinhado (veja exemplo);
- Novo método SetRightMargin;
- Novo método SetUnderline;
- Novo método Writer (mesmo que o Write da FPDF), permite alterar o 
  estilo da fonte em um mesmo parágrafo (só alinhamento à esquerda);
- Novo método Code25 para criar códigos de barras para boletos 
  bancários (veja exemplo);
